import { Container } from '@/components/ui/container';

interface Props {

}

export const Contact = ({}: Props) => {
  return (
    <section>
      <Container>
        d
      </Container>
    </section>
  )
}